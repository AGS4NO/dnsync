name: Test Action

on:
  pull_request:
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  pull-requests: write

jobs:
  unit-tests:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - uses: actions/setup-go@v6
        with:
          go-version: '1.24'
      - run: go test -v ./...

  dns-plan:
    runs-on: ubuntu-latest
    needs: unit-tests
    if: github.event_name == 'pull_request' || github.event_name == 'workflow_dispatch'
    steps:
      - uses: actions/checkout@v6
      - name: Plan YAML config
        uses: ./
        with:
          dnsimple-token: ${{ secrets.DNSIMPLE_TOKEN }}
          dnsimple-account-id: ${{ secrets.DNSIMPLE_ACCOUNT_ID }}
          config-file: dns.yaml
          mode: plan
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      - name: Plan BIND config
        uses: ./
        with:
          dnsimple-token: ${{ secrets.DNSIMPLE_TOKEN }}
          dnsimple-account-id: ${{ secrets.DNSIMPLE_ACCOUNT_ID }}
          config-file: dns.zone
          config-format: bind
          mode: plan
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

  dns-apply:
    runs-on: ubuntu-latest
    needs: unit-tests
    if: github.event_name == 'push' && github.ref == 'refs/heads/main'
    steps:
      - uses: actions/checkout@v6
      - name: Apply YAML config
        uses: ./
        with:
          dnsimple-token: ${{ secrets.DNSIMPLE_TOKEN }}
          dnsimple-account-id: ${{ secrets.DNSIMPLE_ACCOUNT_ID }}
          config-file: dns.yaml
          mode: apply
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      - name: Apply BIND config
        uses: ./
        with:
          dnsimple-token: ${{ secrets.DNSIMPLE_TOKEN }}
          dnsimple-account-id: ${{ secrets.DNSIMPLE_ACCOUNT_ID }}
          config-file: dns.zone
          config-format: bind
          mode: apply
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
